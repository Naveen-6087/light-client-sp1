[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
optimizer = true
optimizer_runs = 200

# Allow reading fixture JSON files from src/fixtures/
fs_permissions = [{ access = "read", path = "./src/fixtures" }]

[profile.default.fuzz]
runs = 256
